import type { CapacitorConfig } from '@capacitor/cli';
const config: CapacitorConfig = {
  appId: 'com.coinstrategy.lab',
  appName: 'CoinLab v0.2',
  webDir: 'dist',
  server: { androidScheme: 'https' },
};
export default config;
